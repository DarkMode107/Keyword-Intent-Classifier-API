# Keyword-Intent-Classifier-API
Classify search keywords into intent categories (Informational, Navigational, Transactional, Commercial Investigation). Useful for SEO, marketing, and ad targeting.
## 🚀 Run Locally ```bash pip install -r requirements.txt uvicorn intent_api:app --reload
